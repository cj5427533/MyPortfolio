# 🚀 Portfolio Web (개인 포트폴리오 사이트)

인터랙티브 웹 포트폴리오의 전체 소스입니다. 정적 페이지이지만 3D/게임 데모, PDF 뷰어, 연락처 폼, 서브 프로젝트 리소스까지 한곳에 담았습니다. 채용 담당자와 협업자가 빠르게 탐색할 수 있도록 구성과 실행 방법을 정리했습니다.

---

## ✨ 주요 특징
- 단일 페이지 포트폴리오: 히어로, 소개, 프로젝트, 수상, 연락처 섹션을 스크롤로 탐색
- 프로젝트 카드 자동 생성: `js/projects-data.js`에 정의한 데이터로 카드/모달을 렌더링 (`js/projects-renderer.js`)
- 3D/게임 데모: Three.js 기반 3D 미로 게임(`Projects/3D_Maze/maze-game.js`), Unity WebGL 빌드(`Projects/Bullet_Game/`)
- PDF/이미지 뷰: 수상 및 제출물 자료를 모달에서 확대해 확인 (PDF.js 사용)
- 연락처 폼: Cloudflare Worker API로 전송 후 Discord 알림을 받는 흐름 (`js/main.js`)
- 반응형 인터랙션: 커스텀 애니메이션/스크롤 효과(`js/animations.js`)와 Tailwind Utility 클래스 기반 스타일

---

## 🗂️ 폴더 구조 (주요 경로)
- `index.html` : 전체 페이지 골격, 섹션 마크업, 스크립트 로드
- `css/styles.css` : 커스텀 스타일 및 Tailwind 보완
- `js/projects-data.js` : 프로젝트 메타데이터(태그, 링크, 썸네일, 색상 테마)
- `js/projects-renderer.js` : 프로젝트 카드/모달 생성, 색상 테마 매핑
- `js/animations.js` : 스크롤/호버 애니메이션, 섹션 등장 효과
- `js/main.js` : 글로벌 인터랙션, 모달 제어, 연락처 폼 제출 로직
- `images/` : 썸네일·프로필·수상 이미지 등 자산
- `Projects/3D_Maze/` : Three.js 미로 게임 스크립트 및 리소스
- `Projects/Bullet_Game/` : Unity WebGL 빌드 산출물(wasm/data/gz 포함)
- `Projects/JEIU Campus Explorer 3D/` : 캠퍼스 익스플로러 3D 프로젝트 산출물(별도 앱 리소스)
- `server.js` : Node 기반 정적 서버(특히 WebGL gzip/wasm 서빙용, 포트 8000)

---

## 🖥️ 로컬 실행
1) Node 18+ 권장  
2) 루트에서 서버 실행  
```
node server.js
```
3) 브라우저에서 `http://localhost:8000` 접속  
- Unity WebGL(`Projects/Bullet_Game`)과 gzip/wasm 리소스는 파일 프로토콜에서 깨질 수 있으니 위 서버 실행을 권장합니다.  
- 연락처 폼은 Cloudflare Worker 엔드포인트(`https://proud-lab-22e2.cj542753315.workers.dev/`)로 POST 요청을 보내며, 인터넷 연결이 필요합니다.

---

## 🛠️ 콘텐츠 수정 가이드
- **프로젝트 카드 추가/수정:** `js/projects-data.js`에 항목을 추가하거나 수정하면 자동으로 카드와 모달이 렌더링됩니다. `colorTheme` 값은 `projects-renderer.js`의 테마 키(`sky`, `indigo`, `purple`, `pink`, `emerald`) 중 하나를 사용합니다.
- **모달/카드 UI 조정:** 카드/모달 레이아웃과 색상 매핑은 `js/projects-renderer.js`, 등장 애니메이션과 인터랙션은 `js/animations.js`에서 관리합니다.
- **연락처 폼:** 제출 로직과 응답 처리 메시지는 `js/main.js` 하단에서 확인할 수 있습니다. 엔드포인트 교체 시 fetch URL만 변경하면 됩니다.
- **이미지/썸네일:** `images/` 폴더에 자산을 추가하고 `projects-data.js`의 `thumbnail` 경로를 업데이트합니다.
- **3D/게임 리소스:** Three.js 미로 게임은 `Projects/3D_Maze/maze-game.js`, Unity WebGL 빌드는 `Projects/Bullet_Game/` 하위에 위치합니다. Unity 빌드는 서버를 통해 서빙해야 정상 동작합니다.

---

## 🌐 배포 팁
- 정적 호스팅(Cloudflare Pages, Netlify, Vercel 등) 가능. 단, wasm/gz 헤더가 필요한 Unity WebGL 빌드는 커스텀 서버(예: `server.js`)나 적절한 MIME 설정이 된 호스팅을 사용하세요.
- 연락처 폼은 Cloudflare Worker에 의존하므로, 도메인/경로를 배포 환경에 맞게 교체하고 CORS 설정을 확인하십시오.

---

## 📌 참고용 하위 프로젝트
- `Projects/JEIU Campus Explorer 3D/` : 캠퍼스 맵/예약 등 3D 웹앱 리소스
- `Projects/Bullet_Game/` : Unity WebGL 슈팅 게임 빌드
- `Projects/3D_Maze/` : Three.js 미로 게임 스크립트

필요 시 각각을 독립적으로 열어 데모하거나, 포트폴리오 메인 페이지에서 연동해 보여줄 수 있습니다.
